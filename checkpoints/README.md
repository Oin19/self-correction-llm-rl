# Checkpoints

Model checkpoints are not stored in Git.

Keep checkpoints in the approved external storage used by the team. Record the checkpoint path/identifier, model, step, date, and corresponding Git commit SHA in experiment logs so runs remain traceable.
